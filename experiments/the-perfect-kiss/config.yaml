training:
  max_steps: 250
  lr: 1e-4
  eval_every: 5
  warmup_steps: 15

batch:
  max_examples: 64
  max_age_minutes: 15

evaluation:
  anchor_provider: hf
  rollback_mode: auto_rollback
  catastrophic_delta: 0.25
